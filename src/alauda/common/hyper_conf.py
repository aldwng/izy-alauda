class HyperConfig:
    def __init__(self, embed_dim, dense_dim, multi_head_num, activation):
        self.embed_dim = embed_dim
        self.dense_dim = dense_dim
        self.multi_head_num = multi_head_num
        self.activation = activation
